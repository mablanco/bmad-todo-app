# API

Python backend scaffold for `bmad-todo-app`.

## Development

Install the project in editable mode with test dependencies:

```bash
python3 -m pip install -e ".[test]"
```

Run the test suite:

```bash
python3 -m pytest
```
